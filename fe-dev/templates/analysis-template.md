# Analysis: {pageId}

## 语义组件树

<!-- 示例（节点后标注关键 Tailwind class）：

└── PageRoot (页面) [p-6 flex flex-col gap-4]
    ├── HeaderNav (头部导航) [h-14 bg-white flex items-center px-6]
    └── HeroSection (主内容区) [py-16 bg-gray-50 rounded-xl]
        ├── HeroTitle (标题) [text-3xl font-bold] [欢迎使用]
        └── CTAButton (按钮) [px-6 py-3 bg-primary rounded-lg] [立即体验]

覆盖层节点（含定位坐标）：
    ├── IllustrationBg (图片) [absolute right-0 top-0] [背景插图]
    └── HeroText (覆盖层) [absolute left-10 top-24] [x=40, y=100]
-->

## Design Tokens

| 变量 | 值 | 说明 |
|------|-----|------|
| <!-- 例：--el-color-primary | #02B3D6 | 品牌主色 --> |

## 资源清单

| 语义名 | 类型 | DSL 节点 ID | 尺寸 | 处理方式 |
|--------|------|------------|------|---------|
| <!-- 例：LogoIcon | LAYER (PNG) | 2:00986 | 120x40 | 下载到 assets/images/ -->
| <!-- 例：MenuIcon | PATH (SVG) | 2:00996 | — | 导出为 SVG 组件 -->
